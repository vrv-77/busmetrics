[project]
name = "busmetric-backend"
version = "0.2.0"
description = "Operational diesel fuel analytics backend for bus depots"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["app"]
testpaths = ["tests"]
